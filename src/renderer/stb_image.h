// stb_image.h — Redirect to ImageDecoder.h
// stb_image ha sido reemplazado por el decodificador propio.
// Este archivo existe solo para compatibilidad de includes existentes.
// Todo el código nuevo debe usar: #include "ImageDecoder.h"
#pragma once
#include "ImageDecoder.h"
